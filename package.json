{
  "name": "@mashiachy/apollo-link-sentry",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "type": "module",
  "scripts": {
    "ts-types": "tsc --emitDeclarationOnly --outDir lib",
    "build": "rimraf lib && node ./esbuild.js && npm run ts-types"
  },
  "peerDependencies": {
    "@apollo/client": "^3.2.3",
    "@sentry/browser": "^7.0.0",
    "graphql": "15 - 16",
    "zen-observable": "^0.8.15"
  },
  "devDependencies": {
    "@apollo/client": "^3.5.6",
    "@sentry/browser": "^7.16.0",
    "@types/zen-observable": "^0.8.3",
    "esbuild": "^0.15.12",
    "graphql": "^15.8.0",
    "rimraf": "^3.0.2",
    "tslib": "^2.0.3",
    "typescript": "^4.8.4"
  }
}
